#include<stdio.h>

int main(){
	/*
	operator + - * /
	
	assignment operators(=)
	
	increment operator(++)
	decrement operator(--)
	
	AND(&&)
	OR(||)
	NOT(!)
	
	greater than(>)
	less than(<)
	greater than or equal to(>=)
	less than or equal to(<=)
	equal to(==)
	
	*/
	int a=3;
	int b=2;
	int c=5;
	int sum=a+b+c;
	printf("the sum is %d",sum);
	
	return 0;
}
